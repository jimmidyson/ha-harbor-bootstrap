tls:
  autoGenerated:
    enabled: true
    engine: cert-manager
    cert-manager:
      existingIssuer: harbor-ca-issuer
      existingIssuerKind: Issuer
master:
  replicaCount: 3
  startupProbe:
    enabled: true
replica:
  replicaCount: 3
  activeReplica: true
  startupProbe:
    enabled: true
